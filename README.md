# Balance Lab

单页原生 `HTML + CSS + JS` 的角色回合制卡牌平衡测试原型。

## 运行

直接用浏览器打开 `index.html` 即可（无需 Node、无需后端）。

## 当前能力

- 2-6 角色开局
- 顺时针回合
- 每回合二选一：抽卡 / 技能
- 6 个角色，不同抽卡代价、不同交互技能、不同胜利条件
- 手动模式 / 自动模式 / 自动走一步
- 底部日志用于复盘平衡性
